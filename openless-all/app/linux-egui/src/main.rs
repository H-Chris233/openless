//! Temporary Linux host entry point.
//!
//! The egui team will replace this stub with the real `eframe::run_native`
//! entry point.  Keeping a binary target now lets CI validate the package and
//! its dependency direction before UI work lands.

fn main() {
    eprintln!(
        "openless-linux-egui host stub: the egui application is supplied by the Linux UI team"
    );
}
